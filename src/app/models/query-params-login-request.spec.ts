import { QueryParamsLoginRequest } from './query-params-login-request';

describe('QueryParamsLoginRequest', () => {
  it('should create an instance', () => {
    expect(new QueryParamsLoginRequest()).toBeTruthy();
  });
});
