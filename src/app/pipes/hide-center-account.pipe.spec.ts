import { HideCenterAccountPipe } from './hide-center-account.pipe';

describe('HideCenterAccountPipe', () => {
  it('create an instance', () => {
    const pipe = new HideCenterAccountPipe();
    expect(pipe).toBeTruthy();
  });
});
