import { PositionCurrencyAccountPipe } from './position-currency-account.pipe';

describe('PositionCurrencyAccountPipe', () => {
  it('create an instance', () => {
    const pipe = new PositionCurrencyAccountPipe();
    expect(pipe).toBeTruthy();
  });
});